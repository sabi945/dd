#include <array>
#include <iostream>
int main() {
    std::array<int,5> baso = {1,2,3,4,5};

    // baso.fill(10);
    // for (int i =0; i < baso.size(); i++) {
    //     std::cout << baso[i] << std::endl;
    // }
    for (int num : baso) {
        std::cout << "hasil nya" <<  num << std::endl;
    }
    for (range-declaration : range-expression                   ) {
    
    }
    return 0;

}